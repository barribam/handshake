# Handshake with Vanilla JS

## Purpose
This project simulates the Client Script communication pattern, the goal is to practice asynchronous data retrieval, JSON parsing,
and UI updates using modern Javascript (promises and async/await)

## Features
- Asynchronous user lookup by ID
- UI state handling (loading, disable actions)
- Error handling
- Clean separation of client and server logic